/*
import { createSelector } from '@ngrx/store';
import { CustomNumberService } from "./Service/customNumber.service";

export const getNumber = (state: CustomNumberService) => state;

export const getCustomNumber = createSelector(
  getNumber,
  (state: CustomNu) => state
);
*/
